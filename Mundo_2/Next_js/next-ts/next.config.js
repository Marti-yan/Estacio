/** @type {import('next').NextConfig} */
const nextConfig = {
    // typescript: {
    //     ignoreBuildErrors: true,      // ignora erros do TS na hora do build do programa
    // }
}

module.exports = nextConfig
