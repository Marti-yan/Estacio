"use strict";
function imprimir(msg, num) {
    console.log(`O conteudo da variável mensagem é '${tmsg}' e da variável numérica é '${tnum}'`);
}
let tmsg = "Yan Martins";
let tnum = 19;
imprimir(tmsg, tnum);
